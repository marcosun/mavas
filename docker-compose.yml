version: "3"

services:
  web:
    image: mavas:latest
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.1"
          memory: 50M
      restart_policy:
        condition: none
        max_attempts: 1
    ports:
      - "18110:80"