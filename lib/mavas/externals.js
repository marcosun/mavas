import Util from './util';

/*
  *@param {Canvas} image [DOM canvas element]
*/
function importData(image) {
  this.canvas = image;
};

function draw() {
  
};

export default {
  importData,
  draw,
}